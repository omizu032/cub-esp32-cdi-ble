# テクニカルノート：デジタルCDIにおける整数型 $\alpha-\beta$ フィルタの理論と実装設計

## 1. $\alpha-\beta$ フィルタの基本原理と理論

$\alpha-\beta$（アルファ・ベータ）フィルタは、レーダーの目標追尾や制御システムで広く用いられる「状態推定フィルタ」の一種です。カルマンフィルタの定常状態（ゲインが一定になった状態）を簡略化したものと位置付けられます。

センサーからの「ノイズを含んだ生の測定値」をそのまま信用するのではなく、システム内部に持った「予測モデル」と「実際の測定値」のズレ（残差）を計算し、そのズレを2つの係数（$\alpha$ と $\beta$）を使って内部モデルにフィードバックします。

### 数学モデル

フィルタは毎サイクル、以下の2つのステップを繰り返します。

**① 予測ステップ (Prediction)**
前回の状態から、今回の状態（位置 $X$ と 速度 $V$）を予測します。本CDIにおいては「位置」が「1回転の周期（時間）」、「速度」が「周期の変化量（加速度）」に相当します。

$$X_{pred}=X_{k-1}+V_{k-1}$$
$$V_{pred}=V_{k-1}$$

**② 更新ステップ (Update)**
実際のセンサー測定値 $Z_k$ が得られたら、予測との誤差（残差 $r$）を計算し、状態を修正します。

$$r=Z_{k}-X_{pred}$$
$$X_{k}=X_{pred}+\alpha \cdot r$$
$$V_{k}=V_{pred}+\beta \cdot r$$

ここで、係数 $\alpha$ は「現在の位置（周期）をどれくらい測定値に寄せるか」、$\beta$ は「現在の速度（加速度）をどれくらい測定値に寄せるか」を決定するパラメータです。

---

## 2. デジタルCDIに実装する上でのメリット

一般的な「移動平均フィルタ（Moving Average）」と比較して、$\alpha-\beta$ フィルタをエンジンの点火制御に用いることには決定的な優位性があります。

* **遅れ（位相遅れ）の極小化:**
  単純な移動平均は過去のデータを平均化するため、エンジンが急加速すると計算上のRPMが実態より遅れて追従します。一方、$\alpha-\beta$ フィルタは「速度（$V$）」というパラメータを持っているため、**「いまエンジンは加速中である」というトレンドを予測に組み込む**ことができ、急加減速時の位相遅れ（追従遅れ）が劇的に改善されます。
* **微小進角領域のジッター完全制圧:**
  3500〜4500rpm付近での「数十$\mu$sの進角」に対し、クランクセンサーや割り込みレイテンシ由来の「数十$\mu$sのノイズ」が乗ることで発生していた点火のブレ（ジッター）を数学的に平滑化し、オシロスコープ上で1本の線に見えるほどの圧倒的な静粛性・安定性をもたらします。
* **計算負荷の圧倒的な軽さ:**
  カルマンフィルタのように重い行列演算や分散の再計算を必要とせず、固定のゲインで計算するため、マイコンの外部割り込み（ISR）という極めてシビアな時間制約（数$\mu$s以内）の中でも余裕で実行可能です。

---

## 3. スーパーカブ用CDIとして実装するための「工夫点」

今回、ホンダ横型エンジン（スーパーカブ90）の制御に最適化し、かつFPUクラッシュを防ぎ、低スペックマイコン（8-bit AVRなど）へも移植可能にするために、以下の高度な工夫を凝らして実装しています。

### ① 浮動小数点を排除した「ビットシフト整数演算」と定数マクロ化

割り込み処理内での `float` 演算はタブーです（処理遅延やクラッシュの原因）。そのため、$\alpha$ と $\beta$ の値を「2のべき乗の逆数」に設定し、除算を高速なビットシフト（論理右シフト）に置き換えました。

初期検証では $\alpha=1/4, \beta=1/16$ と設定していましたが、加減速時や遅角・進角の切り替わりポイントにおける「パルス追従性」と「滑らかさ」の最適解を実機オシロスコープ検証から導き出し、現在は以下の係数を採用しています。

* $\alpha = 0.5 \ (1/2)$ ➔ `residual >> 1`
* $\beta = 0.125 \ (1/8)$ ➔ `residual >> 3`

さらに、今後のチューニングやハードウェア移植（Arduino Nanoなど）を容易にするため、シフト量をハードコーディングせず、プログラム先頭でマクロ定数として一元管理する設計としています。

```cpp
#define AB_FILTER_ALPHA_SHIFT 1  // α係数 (1/2)
#define AB_FILTER_BETA_SHIFT  3  // β係数 (1/8)

// ISR内の更新処理
ab_interval = predicted_measurement + (residual >> AB_FILTER_ALPHA_SHIFT);
ab_velocity = ab_velocity + (residual >> AB_FILTER_BETA_SHIFT);

```

これにより、32ビット整数（`int32_t`）の単純な加減算とビットシフトのみで状態推定が完了し、計算時間はわずか数クロックに収まります。

### ② マイコン内部の「仮想フライホイール」の構築

スーパーカブのクランクセンサーは「1回転に1パルス（捨て火方式）」です。つまりデータのサンプリングレートはエンジンの回転そのものです。
単気筒エンジンの1回転周期は、燃焼ストロークによる加速と圧縮ストロークによる減速、さらにカムチェーンのたわみ等によって1サイクルごとに細かく振動しています。
このフィルタは、マイコン内部に物理的なフライホイールよりも重い「電子的な仮想フライホイール」を構築する役割を果たし、機械的な振動ノイズ（カムチェーンのバタつき等）を無視して「真のクランク回転数」だけを抽出します。

### ③ 始動時・失火時の「フェイルセーフ強制リセット」

$\alpha-\beta$ フィルタの弱点は「予測不能な極端な外乱が起きた時に、元の状態に戻るまで時間がかかる」ことです。キックスタート時や、何らかの理由で失火して回転が急激に落ち込んだ場合、フィルタが過去のモデルを引きずると点火タイミングが破綻します。
これを防ぐため、以下の「外乱リジェクト論理」を組み込んでいます。

```cpp
int32_t error = measurement - ab_interval;
if (error < 0) error = -error; // 絶対値

// 測定値が現在のモデルから50%以上乖離した場合（異常時）
if (error > (ab_interval >> 1)) {
    ab_interval = measurement; // フィルタを破棄して現在値で上書き
    ab_velocity = 0;           // 加速度をリセット
}

```

予測周期に対して実際の周期が **50%以上（>> 1）ズレた場合** は、「ノイズではなくエンジンの状態が劇的に変わった（始動、エンスト寸前、異常失火）」とみなし、フィルタの蓄積を瞬時に破棄して最新の生データに食いつき直すフェイルセーフを設けています。

### ④ 進角計算とRPM参照への一元化

ジッターの撲滅を完全なものにするため、単に「次回のタイマー予測値」にフィルタをかけるだけでなく、**「マップを参照するためのRPM計算」にも、生データではなくフィルタリング済みの `ab_interval` を使用**しています。

```cpp
current_rpm = 60000000 / ab_interval;

```

これにより、「マップの参照点が行き来して補間計算がブレる」という上流のノイズと、「ディレイタイマーの発火時間がブレる」という下流のノイズを、同時にかつ完全に鎮圧しています。

---

**【総括】**
この「整数型 $\alpha-\beta$ フィルタ」と「レスキュー点火（強制ベース点火）」、そしてESP32特有の「デュアルコア完全分離」の組み合わせは、計算資源とシビアな時間制約の中で過渡応答を制御する上で、現在考えうる最もエレガントかつ実戦的なアルゴリズムです。これにより、スーパーカブのデジタルCDIは「実験室レベル」から「実用/量産レベル」の安定性とリアルタイム性へと到達しました。
